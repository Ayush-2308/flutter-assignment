# Office Dashboard

A responsive Flutter web and mobile office dashboard assignment.

## Run

```powershell
flutter pub get
flutter run -d chrome
```

For mobile:

```powershell
flutter run -d android
flutter run -d ios
```
